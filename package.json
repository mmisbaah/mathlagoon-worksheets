{
  "name": "mathlagoon-worksheets",
  "version": "2.0.0",
  "private": true,
  "scripts": {
    "test": "node --test tests/*.test.cjs",
    "build": "npm test && node scripts/build.cjs"
  },
  "devDependencies": {
    "jsdom": "^30.0.1"
  }
}
